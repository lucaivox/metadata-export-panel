<div id="spinner-container">
    <div class="spinner"></div>
    <div id="spinner-text"><b>Exporting album ivox400</b></div>
    <div id="spinner-text2">Exporting 1 of 5 albums</div>
</div>
<style>

#spinner-container {
    position:fixed;
    width:100%;
    left:0;right:0;top:0;bottom:0;
    background-color: rgba(255,255,255,0.7);
    z-index:9999;
    display:none;
}

.spinner{
   position: absolute;
   top: 35%; left:50%;
   transform: translate(-50%);
   width: 56px;
   height: 56px;
}

#spinner-text{
   position: absolute;
   top: 45%; left:50%;
   transform: translate(-50%);
   height: 56px;
}

#spinner-text2{
   position: absolute;
   top: 50%; left:50%;
   transform: translate(-50%);
   height: 56px;
}

.spinner::before,
.spinner::after {
   content: '';
   width: 100%;
   height: 100%;
   animation: spinner-rfi6tk 1.2s infinite linear;
   box-sizing: border-box;
   border: 11.2px solid rgba(71,75,255,0.1);
   border-radius: 50%;
   position: absolute;
}

.spinner::before {
   animation-direction: reverse;
   border-right-color: #474bff;
   right: calc(50% - 5.6px);
}

.spinner::after {
   border-left-color: #474bff;
   left: calc(50% - 5.6px);
}

@keyframes spinner-rfi6tk {
   0% {
      transform: rotate(0deg);
   }

   50%, 100% {
      transform: rotate(360deg);
   }
}
</style>